services:
  postgres:
    image: postgres:16-alpine
    container_name: scalora-postgres
    environment:
      POSTGRES_DB: scalora
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - scalora_postgres:/var/lib/postgresql/data

volumes:
  scalora_postgres:
